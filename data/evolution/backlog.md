# MyCR Evolution Backlog

This file is the working queue for improvements discovered while running MyCR.
Keep entries concrete enough that a later agent can implement or reject them.

## Active Candidates

| Date | Area | Observation | Proposed Change | Status |
| --- | --- | --- | --- | --- |
| 2026-05-26 | Repo layout | Reports and skill logic were split between the installed skill directory and `trpc-agent-go/.vscode`. | Make `/Users/guoqizhou/projects/github/mycr` the canonical home for skill, scripts, report history, and website deployment. | Done |
| 2026-05-26 | Reports | Old reports were only local HTML/JSON files and could not be browsed from the personal site. | Archive JSON/HTML under this repo and expose them through the `/mycr` Astro site. | Done |
| 2026-05-26 | Paths | The skill used `../../github/trpc-agent-go` and `.vscode/mycr-reports` as hardcoded defaults. | Resolve the target checkout through `MYCR_TARGET_CHECKOUT`, repo-relative fallback, and one documented absolute fallback; write reports to this repo. | Done |
| 2026-05-26 | Report UI | Expanded PR details were rendered as many vertically stacked field boxes, which made long reports hard to scan. | Group PR details into narrative sections, keep review outcome/risk/CI in a compact side rail, and keep inline comments as a distinct evidence block. | Done |
| 2026-05-26 | Report UI | PR cards still had similar visual weight, making it hard to know which PRs deserved careful reading. | Add reading recommendations, priority colors, and inline keyword highlights to full HTML reports. | Done |
| 2026-05-26 | Review gates | MyCR could skip PRs because GitHub still showed `CHANGES_REQUESTED` even after the author fixed and resolved the concrete threads. | Require thread-level blocker audits, forbid aggregate `CHANGES_REQUESTED` as a standalone skip reason, and display structured concrete blockers in reports. | Done |
| 2026-05-26 | Review coverage | Reviewable PRs could be hidden by candidate ordering, broad skip groups, or ambiguous soft-gate reasoning. | Maintain a run-level reviewability ledger and require every reviewable PR to be processed or explicitly reported as `not_reached` with a concrete reason. | Done |
| 2026-05-27 | Own PR handling | MyCR previously skipped the authenticated user's PRs, which hid issues in the maintainer's own changes and encouraged self-review comments if treated like external PRs. | Add an own-PR maintenance path that checks out pushable own PR branches in separate worktrees, fixes valid findings directly, reports them as `maintained`, and never self-approves, self-merges, or posts self-review comments. | Done |
| 2026-07-03 | Review gates | Some PRs are technically reviewable but still require a human maintainer, mentor, code owner, or product owner to choose the accepted merge candidate, such as mentorship-program issues, competing student submissions, security-boundary work, broad public APIs, or architecture/product decisions. | Add a first-class `manual_review` blocker that allows MyCR to audit and comment, but forbids `LGTM` approval and merge until a human explicitly selects the PR as merge-ready. | Done |
| 2026-07-06 | Review gates | Recent merges showed that `manual_review` was not rechecked as an unavoidable final-action predicate, so a contest, security-boundary, or broad verification-contract PR could become clean after CI and still be approved and merged by MyCR. Non-member PRs also need a way to be reviewed without letting MyCR perform the final merge. | Recheck `manual_review` immediately before approval and merge, treat MyCR's own `LGTM` as insufficient evidence for human selection, and add `merge_policy: human_required` for PR classes whose final merge must be performed by a human. | Done |
| 2026-05-26 | GitHub review posting | The GitHub connector requires a non-empty review body for `COMMENT` reviews, which risks creating top-level review text when MyCR should submit inline-only comments. | Add a small MyCR helper that submits inline-only reviews through `gh api`, validates created review/comment URLs, and falls back to the connector only when an inline-only API path is unavailable. | Open |
| 2026-05-27 | GitHub review posting | `POST /pulls/{number}/reviews` returned a COMMENTED review for #1859 but did not create a visible inline comment, requiring a second direct `/pulls/{number}/comments` call and manual URL validation. | Implement a report-time and post-time invariant that every submitted inline comment has a matching `pulls/comments` URL; retry through the direct review-comment endpoint when the review wrapper produces no comments. | Open |
| 2026-05-27 | Review gates | A PR skipped for pending CI (#1874) turned green during the same run and needed to be promoted back into the candidate queue manually. | Add a late pre-report requeue pass that refreshes every non-draft external PR with prior pending/missing checks and automatically reviews any item that became green. | Open |
| 2026-05-27 | Comment blockers | #1400 had a concrete changed-line compatibility blocker, but GitHub returned `Issue is locked`, leaving the finding only in the report. | Represent comment-delivery failures as first-class blocked outcomes with the attempted payload, API error, and recommended maintainer escalation path. | Open |
| 2026-05-29 | Run orchestration | The full run required repeated manual `gh`/GraphQL refreshes for PR metadata, checks, threads, merge state, and own-PR CI after each action, and the report had to be assembled from many temporary JSON files. | Add a run-state collector that stores one normalized ledger per PR, refreshes processed PRs before reporting, and emits the report JSON skeleton directly from that ledger. | Open |
| 2026-06-01 | Run efficiency | MyCR was spending heavy review, diff, thread, and report-writing effort on open PRs whose actionable state had not changed since the prior run. | Add an incremental planning contract and `scripts/mycr-incremental-plan.mjs` so each run still builds a lightweight full-open-PR ledger, but only promotes changed/new/not-reached/stale PRs into the expensive heavy-review path. | Done |
| 2026-06-02 | Report assembly | The run still needed manual stitching of final comment refreshes, local test caveats, and per-PR review narratives after the ledger and inline comments were already known. | Extend the run-state collector to persist processed-PR final refresh results, test command outcomes, and submitted review URLs, then generate the commented/maintained/blocked report sections from that normalized data. | Open |
| 2026-06-02 | Run efficiency | The periodic freshness sweep still promoted unchanged stale PRs into full `heavy_review`, and the cheap index could include large comment/thread bodies. | Add `refresh_probe`, per-PR `metadata_cache_key`/`cache_mode`, report-outcome blocker reuse, and a cache manifest so unchanged PRs reuse full metadata while changed fingerprints still promote immediately. | Done |
| 2026-06-02 | Lightweight index | Bulk GraphQL and `gh pr list --json statusCheckRollup` both returned 504 during the run, so the cheap index had to fall back to an ad hoc per-PR collector and a one-time fingerprint migration baseline. | Add a repo-managed lightweight collector with bounded per-PR concurrency, retries, stable check fingerprints that ignore timing/order noise, and explicit fingerprint schema migration support. | Open |
| 2026-06-03 | Agent delegation | Three review subagents hit provider 429 retry limits, leaving the main run to repeat the intended fallback audit and remember which PRs had delegated attempts. | Persist subagent attempt/result state per PR, including 429 failures, fallback reviewer, and whether the fallback satisfied the skill's delegation requirement. | Open |
| 2026-06-03 | Test reporting | Own-PR OpenClaw broad package tests failed for environment-specific reasons while targeted PR tests and CI were green; the report had to explain that distinction manually. | Add structured test outcomes with `passed`, `failed_environmental`, and `not_applicable` classifications so generated reports can distinguish product regressions from local config/watch-path noise. | Open |
| 2026-06-03 | Final audit | The final PR comment/thread refresh used a handwritten GraphQL query for the processed candidates. | Promote final processed-PR audit into a script that fetches state, latest comments, reviews, unresolved threads, and merge metadata, then feeds report generation directly. | Open |
| 2026-06-03 | Incremental planning | The GraphQL cheap-index call failed with 502, and the ad hoc REST fallback produced fingerprints that did not match the previous report schema, so the planner promoted all 40 open PRs to `heavy_review`. | Make the fallback collector emit the same normalized fingerprint schema as the primary collector, including review-decision and thread/comment fingerprints, and add a migration guard that explains unavoidable cache invalidation. | Open |
| 2026-06-08 | Run state | The previous report lacked reusable `run_state.pull_requests`, so the planner promoted all 32 open PRs to `heavy_review`. | Make report generation include normalized `run_state.pull_requests` from planner output and verify future reports have a non-empty pull request map. | Open |
| 2026-06-08 | Review comments | Collecting submitted review comment URLs initially used query parameters as request fields, causing a GitHub API 422 response. | Add a repo helper for GET pull review comments with pagination, author/time filtering, and saved comment URLs. | Open |
| 2026-06-08 | Local test portability | macOS sandbox tests exposed a `/var` to `/private/var` symlink canonicalization failure, and MyCR had to separate genuine cross-platform regressions from Linux-only sandbox checks. | Add OS-aware local test classification and capture GOOS or unsupported-module notes in the report ledger. | Open |
| 2026-06-09 | Targeted review mode | A user asked MyCR to process only one GitHub pull review comment URL, and the run needed manual comment/thread fetching, current-head verification, and report assembly. | Add a helper that accepts a pull review comment URL, resolves the PR/thread, verifies whether the finding still applies on the latest head, optionally resolves fixed own threads, and emits a targeted report skeleton. | Open |
| 2026-06-18 | Report assembly | A full sweep with no GitHub mutations still required manual synthesis of blocked candidate narratives from the ledger, review-thread audits, and subagent outputs. | Add a blocked-run report generator that consumes the normalized open-PR index, check summary, thread audit, and subagent result notes, then emits `blocked` and `skipped_groups` report sections without hand-stitching. | Open |
| 2026-06-22 | Run orchestration | A full run with real approvals/merges still required an ad hoc per-PR fallback collector after bulk GraphQL 504, manual inline-comment URL validation, compact CI polling, and manual report JSON assembly. | Promote the fallback collector into a repo-managed runner that records a normalized action ledger, validates inline comments, detects head moves before approval, waits or reports pending CI, and emits the report JSON directly. | Open |
| 2026-06-23 | Run orchestration | This full run again needed ad hoc per-PR fallback after bulk GraphQL 504, manual candidate promotion for fixed/outdated threads, manual CI polling for #1978, and hand-built report JSON from temporary metadata. | Implement the repo-managed runner promised by the backlog: bounded fallback collector, final candidate requeue, inline-comment URL capture, processed-PR audit, and direct report JSON emission from a normalized ledger. | Open |
| 2026-06-23 | PR readiness gates | PR #1983 showed two missed edge cases: close/reopen left the conversation locked by `github-actions[bot]`, and undeduplicated GraphQL `statusCheckRollup` still exposed an old `go test (openclaw/go.mod)` failure even though `gh pr checks` showed the latest effective checks green. | Add locked-state fields and lock/unlock timeline events to the lightweight ledger, treat locked conversations as comment-delivery risk instead of an invisible skip, and compute CI readiness from latest effective current-head checks rather than raw historical rollup entries. | Open |
| 2026-06-23 | Final audit | A new PR (#2005) appeared after the initial open-PR ledger but before report generation, and `gh pr checks` transiently returned an empty result for #1983 before succeeding on retry. | Add a final open-PR diff pass that detects PRs created during the run, collects their minimal gate metadata, and either processes or reports them; make the collector retry empty/failed `gh pr checks --json` responses before accepting missing checks. | Open |
| 2026-06-23 | Run-state persistence | The 20:25 report archived an action ledger but left `run_state.pull_requests` empty, so the next run could not reuse fingerprints and conservatively marked every open PR as `heavy_review`. This run also briefly resolved a still-valid #1988 thread before xhigh review caught the prefixed agentic-filter case. | Make report generation consume the planner's `run_state.pull_requests` object directly and add a pre-archive assertion that it contains every final open PR; add a thread-resolution guard that requires final xhigh/main-thread confirmation before resolving an existing blocker. | Open |
| 2026-06-26 | Report automation | A full run with 4 merges and 5 new comments still required hand-maintained action-ledger lines, manual subagent-result transcription, repeated ad hoc final check/thread loops, and manual JSON report assembly. One processed comment (#2000) had to be backfilled into the ledger before archiving. | Implement a first-class run finalizer that ingests action ledger events, submitted review URLs, subagent findings, final `gh pr checks`, unresolved thread audits, and final open PR diff, then emits `approved`, `commented`, `blocked`, `skipped_groups`, and `run_state.pull_requests` without manual stitching; fail archive if any submitted review URL is missing from the ledger. | Open |
| 2026-06-28 | Incremental planning | The previous report had a non-empty `run_state.pull_requests`, but it lacked the newer check/thread/comment/lock/file fingerprints, so the planner marked all 36 open PRs as `heavy_review` with `missing_required_fingerprint`. | Add explicit run-state schema migration/backfill: when older reports lack newly required fingerprints, compute a lightweight compatibility fingerprint from available fields, mark only uncertain entries for refresh probes, and explain intentional cache invalidation in the plan instead of silently forcing a full heavy sweep. | Open |
| 2026-06-29 | Run-state persistence | The previous successful report used singular fingerprint fields (`check_fingerprint`, `review_thread_fingerprint`, `comment_review_fingerprint`) while the planner expects plural fields (`checks_fingerprint`, `review_threads_fingerprint`, `comments_fingerprint`), so this run again promoted every open PR to `heavy_review`; two PRs (#2034 and #2028) also changed from draft/old head to ready/current head during the run and needed a manual final-state patch before report generation. | Add a report/run-state finalizer that emits both compatibility aliases for one migration window, recomputes final lightweight fingerprints from the last open-PR snapshot after actions, and fails archive if any final open PR head/readiness differs from `run_state.pull_requests`. | Open |
| 2026-06-30 | GitHub review posting | PR #2028 reproduced the close/reopen lock edge case: `POST /pulls/{number}/reviews` returned `422 Issue is locked` even for an ADMIN account, so blocking review comments would have disappeared without a manual unlock/relock sequence. | Add a locked-PR review helper that records lock timeline, verifies permission, temporarily unlocks only when policy allows, posts the review, validates inline comment URLs, restores the original lock state, and otherwise emits a first-class comment-delivery blocker. | Open |
| 2026-06-30 | Report finalization | This full run merged 6 PRs, approved 1 codecov-blocked PR, and posted 2 changes-requested reviews, but final report assembly still required manual status refreshes, comment URL lookup, local-test caveat classification, and JSON stitching from temporary files. | Implement the run finalizer from the action ledger: consume `gh` action results, subagent outputs, current open-PR light ledger, check summaries, local test outcomes, lock/unlock events, and submitted review URLs, then emit archive-ready report JSON and `run_state.pull_requests` automatically. | Open |
| 2026-07-01 | GitHub inline review posting | Creating a review through `gh api POST /pulls/{number}/reviews` with `comments[].line`/`side` silently produced empty pending reviews instead of inline comments; the run had to delete those pending reviews, compute diff `position`, create individual pull review comments, and submit/verify `REQUEST_CHANGES` separately. | Add a MyCR `post-inline-review` helper that parses PR diff positions, rejects missing anchors before writing, posts comments through the correct endpoint, submits the review event, deletes empty pending reviews on failure, validates every comment URL, and handles locked PR unlock/relock in one transaction. | Open |
| 2026-07-02 | Run finalization | This full run merged #2026/#2039 and posted 10 inline blockers, but still needed a temporary review-comment script fix, manual diff-position preflight, separate request-changes submission, and hand-built report sections; own PR #2052 also showed that green `gh pr checks` and Codecov patch-comment evidence can diverge in the maintainer audit. | Promote the temporary posting/finalizer logic into a repo-managed command that atomically preflights anchors, posts comments, submits the review event, captures comment URLs, refreshes final open PRs after merges, reconciles effective checks with Codecov comments, and emits the archive-ready report plus `run_state.pull_requests`. | Open |
| 2026-07-03 | Run orchestration | This full run again hit unstable bulk GitHub collection, required manual bounded per-PR fallback, subagent result stitching, review creation, separate review-comment URL lookup, and hand-built `run_state.pull_requests`; #2062 also required a local test worktree reuse path before merge. | Add a first-class `mycr run finalize` command that ingests the lightweight ledger, action events, subagent findings, local test outcomes, final open-PR refresh, and created review IDs; it should fetch/validate inline comment URLs, mark commented PR fingerprints stale, emit archive-ready JSON/HTML, and fail if final open PRs or review URLs are inconsistent. | Open |
| 2026-07-03 | Lightweight index | The ad hoc per-PR collector hit a transient GitHub GraphQL `EOF`, and running `mycr-cache-manifest` in parallel with planner creation failed before `plan.json` existed. | Promote the per-PR collector into a repo-managed command with retry/per-PR cache/resume support, and wrap planner plus cache-manifest generation in one ordered command so dependency sequencing cannot race. | Open |
| 2026-07-04 | Run orchestration | An explicit full run still produced carry-forward entries when the incremental planner was given prior state, so the operator had to rerun planning without previous state; the canonical skill also requested subagents, but the available tool policy forbade spawning without an explicit user request. | Add a single `mycr run --full` command that forces every open PR into heavy review, refreshes the cache manifest in order, and records delegation policy as a run capability so reports can distinguish skipped delegation from review coverage. | Open |
| 2026-07-04 | Report quality | The latest archived report rendered but contained many empty per-PR detail fields, review-action text inside PR implementation fields, non-canonical display statuses, skipped groups without item details, and string follow-ups that the HTML renderer displayed as empty cards. | Keep `scripts/report-quality.mjs` in the archive/verify path, expand it into a versioned report schema with fixtures, and make the run finalizer fail before publishing when rendered PR details, skipped groups, or follow-ups cannot be shown as an auditable engineering brief. | In Progress |
| 2026-07-04 | Self-evolution | The full flow recorded only a generic backlog note and did not force an independent report QA pass after publishing-quality HTML was generated. | Make every meaningful run perform post-report QA: normalize/check latest report, inspect HTML-visible sections, invoke a fresh xhigh reviewer when explicitly requested or allowed, record concrete friction in backlog, and implement low-risk local fixes before commit/push. | In Progress |
| 2026-07-05 | Run finalization | This heartbeat full run merged #2080/#1526 and produced a quality-checked report, but the operator still had to hand-stitch processed PR narratives, final open-PR grouping, local test outcomes, and skipped-group reasoning from temporary `gh` snapshots. | Implement a repo-managed `mycr run finalize` command that ingests the effective-check ledger, processed PR action events, final PR views, local validation commands, and evolution notes, then emits archive-ready JSON with rich processed entries and itemized skipped groups before `archive-report` runs. | Open |
| 2026-07-05 | Review thread handling | This run verified #2041 stale blockers as fixed, then resolved six review threads with one-off GraphQL calls before approval; the resolved-thread evidence had to be copied into the report manually. | Add a `mycr resolve-fixed-threads` helper that consumes a reviewed PR's thread audit and verified-fix notes, resolves only approved thread IDs, records before/after unresolved counts, and emits report-ready evidence for the run finalizer. | Open |
| 2026-07-05 | Own PR maintenance | This run had to manually distinguish a stale remembered own PR (#2078, already merged) from the current own PR (#2098), create a dedicated worktree, repair coverage, push to the fork branch, then refresh mergeability and checks without self-reviewing. | Add an `own-pr-maintain` runner step that discovers current authenticated-user PRs, detects conflicts/check failures, checks out pushable head branches in isolated worktrees, applies focused repairs, pushes, refreshes CI/mergeability, and records the result as `maintained` while forbidding self-approval and self-merge. | Open |
| 2026-07-05 | Xhigh QA integration | The candidate xhigh reviewer returned additional valid blockers after initial request-changes comments had already been posted; the operator then had to reconcile those findings back into PR comments and the report JSON manually. | Store subagent results as structured run artifacts, require a reconciliation pass before report archive, and fail finalization if any xhigh `needs-blocking-review` item is not represented in GitHub comments and the `blocked` report section. | Open |
| 2026-07-05 | Report HTML QA | The report needed a separate static HTML scan plus a fresh xhigh report-QA reviewer after archive; only then did #2098's latest all-green CI state get refreshed into the final HTML. | Add a post-archive QA command that scans generated HTML for rendered bad tokens, verifies required PR/action text appears, refreshes recently pushed own-PR checks, optionally invokes an xhigh report reviewer, and reruns archive before commit when report facts changed. | Open |
| 2026-07-06 | Own PR maintenance | A new own PR (#2106) appeared after the initial full-run ledger, already had a hard `check go mod tidy` failure, and had to be discovered only during final refresh before report generation. | Add a final own-PR discovery pass before report archive that diffs current open PRs against the initial ledger, promotes new authenticated-user PRs into maintenance, repairs deterministic CI failures, and records refreshed head/check state. | Open |
| 2026-07-06 | Report HTML QA | The manual HTML scan initially looked for `PR #<number>` labels while the renderer exposes PR links as `#<number>`, producing false missing-section alarms even though the HTML had rendered the cards. | Move the HTML contract scan into a repo script with renderer-aware selectors/text checks for approved, commented, maintained, skipped, and self-evolution sections instead of ad hoc substring probes. | Open |
| 2026-07-06 | Report validators | A fresh xhigh report QA found publish-blocking contract defects that `report-quality.mjs` missed: own PRs hidden under `own_pr_no_self_review`, aggregate `CHANGES_REQUESTED` without thread evidence, unaudited soft-CI skips, duplicate skipped buckets, and visible CI disagreeing with `run_state`. | Extend report-quality to enforce one bucket per PR, forbid final `own_pr_no_self_review`, require thread/check evidence for CHANGES_REQUESTED and soft-CI blockers, require maintained `direct_fixes`, and fail when visible processed CI summaries diverge from `run_state`. | Open |
| 2026-07-06 | Report validators | The 10:03 report QA found three additional weak blocker classes: bot/author replies were classified as `human_review`, locked PRs showed CI failures only in prose, and `CodeRabbit=PENDING` was mislabeled as a soft-CI exception. | Add report-quality rules that reject human-review blockers from bots or the PR author, require concrete CI blockers for locked PRs with non-green checks, and forbid pending checks under `soft_ci`; the first guard was implemented in `scripts/report-quality.mjs`, with richer original-thread/latest-response validation still open. | In Progress |
| 2026-07-07 | Report data hygiene | This comment-only run found that truncating CodeRabbit/GitHub review bodies by UTF-16 index can split an emoji surrogate pair and make the generated run JSON fail under `jq` or any strict JSON parser. | Move the temporary code-point truncation and invalid-Unicode sanitization into the repo-managed GitHub collector/finalizer, and add a fixture with emoji-heavy review text so report generation cannot publish invalid JSON. | Open |
| 2026-07-07 | Report QA | The independent report review found publish-quality gaps that were easy to miss locally: process narration in `technical_background`, raw skipped-group enum titles, empty optional blocker fields, and processed PRs carrying stale CI blockers in `run_state`. | Keep the new renderer and validator guards, then move the same checks into the future run finalizer so report JSON is corrected before archive rather than after an independent QA pass. | In Progress |
| 2026-07-07 | Effective checks | This comment-only run initially treated #2128 as clean from an incomplete check sample, while the current GitHub checks showed `lint` failing. | Make the finalizer store current effective checks from the same source of truth used for gating, paginate raw check collection, and fail report generation if a skipped/eligible PR has contradictory check summaries. | Open |
| 2026-07-07 | Comment finalization | #2125 was commented on an old head for a real go-apidiff blocker, then the author pushed a fix before report archive; the report had to manually mark the comment as fixed/outdated instead of leaving it as a live blocker. | After every posted comment, refresh the PR head/checks/relevant diff before archiving and classify the comment as `still_blocking`, `fixed_after_comment`, or `head_changed_needs_reaudit`. | Open |
| 2026-07-07 | Report validators | The post-archive report QA found skipped human-review blockers that were clipped mid-word or reduced to `Blocking issues found`, plus public report fields that exposed runner implementation details instead of maintainer-facing facts. | Keep the new `report-quality` vague/clipped blocker guard, extend it to run-state blockers, and split public report facts from private run diagnostics in the finalizer. | In Progress |
| 2026-07-08 | Public report hygiene | The post-archive report QA found that the public JSON/HTML could expose local run paths, cache source fields, run-state fingerprints/cache keys, and compact PR cards that summarize the review action instead of the PR's technical problem. | Add a public-report sanitizer before archive that strips local paths/private run diagnostics while keeping audit facts, store incremental fingerprints in a non-public state file if still needed, and add renderer tests for compact summaries and `not_reached` skip labels. The sanitizer and public artifact gate are now in place; non-public state storage and richer renderer fixtures remain. | In Progress |
| 2026-07-08 | Report validators | A post-report QA pass found two validation blind spots: public artifacts still exposed nested local execution state (`target_worktree_dirty_preserved`), and `--public` checking was too broad, so source reports could skip the required `run_state.pull_requests` guard. | Recursively strip known private nested keys during archive, forbid them in public artifacts, and distinguish direct public JSON checks from source-report `--public` checks so source reports must still carry the full run ledger. | Done |
| 2026-07-09 | Public report wording | The independent post-archive QA found internal-facing `source ledger` wording in the public report and a skipped PR whose `reviewDecision=APPROVED` evidence could be misread as a new MyCR approval. | Replace public wording with maintainer-facing coverage language, require skipped APPROVED review decisions to state they are pre-existing and that no MyCR approval was submitted, and fail public checks if `source ledger` appears again. | Done |
| 2026-07-09 | GitHub comment verification | Querying pull review comments with `gh api -f per_page=100` changes the request to POST and can hit the create-comment endpoint with a 422 instead of fetching comments. | Add a `mycr gh comments` helper that always uses `--method GET`, quoted pagination URLs, review-id filtering, and output suitable for report finalization. | Open |
| 2026-07-09 | Comment-only reporting | A comment-only run can deeply check a green PR and find no new issues, but putting it into a generic skipped bucket makes the HTML look like review coverage is missing. | Add a first-class `reviewed_clean_deferred` outcome in the finalizer and renderer so clean-but-not-approved PRs are visible without implying LGTM or merge readiness. | Open |
| 2026-07-09 | Public report sanitizer | The public artifact gate correctly forbids internal cache/fingerprint wording, but it also caught a legitimate telemetry-code "message fingerprinting" description in a PR summary. | Split forbidden wording into private-run diagnostics versus code-domain terminology, or require explicit maintainer-facing rewrites only for fields that describe MyCR internals. | Open |
| 2026-07-09 | Report QA fixtures | The post-report QA found three reusable gaps: public artifacts exposed `target_checkout_dirty_preserved`, zero-count status bars rendered as visible 3% bars, and skipped PRs repeated generic not-reached reasons. | The private-field sanitizer and zero-bar renderer fix are in place; add fixtures that cover nested private fields, zero-count bars, and item-specific skip reasons so future reports fail before publish. | In Progress |
| 2026-07-10 | Report outcome consistency | Post-report QA found a clean-deferred wording conflict: a PR with no new code finding still had a `manual_review` gate, but overview/follow-up text made it read like an unconditional clean result. | Keep the new validator that rejects hard blockers (`manual_review`, CI, merge conflict) combined with "clean/no blocker" wording, and add a fixture for manual-review-deferred-after-clean-code-review. | In Progress |
| 2026-07-11 | Public report hygiene | The comment-only report initially exposed an internal GitHub REST attribution field name inside `skill_evolution`, even though the actual public-facing fact was simply "no visible GitHub App attribution". | Keep public evolution notes maintainer-facing, forbid the raw REST attribution field name in public artifacts, and later split private run diagnostics from public self-evolution copy in the finalizer. | In Progress |
| 2026-07-11 | Public report wording | The evening comment-only report initially exposed internal orchestration vocabulary inside public follow-up and self-evolution copy, even though the actionable maintainer-facing point was only state refresh and duplicate-comment suppression. | Add a final public-copy normalization step before archive so follow-up and evolution fields are rewritten as maintainer-facing outcomes, and keep the public artifact gate as the last publish blocker. | In Progress |
| 2026-07-12 | Review planning | The full run's planning step still promoted all 70 open PRs into the expensive review queue because the previous-state comparison could not reuse stable head/check/comment signals, so the operator had to manually narrow to four current candidates. | Teach the finalizer to persist reusable state in a private-safe form and make the planner default unchanged PRs to status refresh only; add a regression where unchanged PRs stay out of `heavy_review` after a comment-only report archive. | Open |
| 2026-07-12 | Report coverage | The 09:00 report initially dropped three PRs that had previous MyCR comments and unchanged heads from both `commented` and `skipped_groups`, so the public report represented fewer PRs than the open-PR ledger. | Add a latest-report coverage check that requires unique processed plus skipped PR numbers to equal `repo_info.open_prs`; classify unchanged previously-commented PRs as `previous_comments_waiting_author`. | Done |
| 2026-07-12 | Run-state completeness | The 09:00 report initially left `mergeable`, lock reason, and many fingerprint/cache fields empty in `run_state`, which would make the next incremental planner distrust the ledger. | Require non-empty source `run_state` cache fields in report-quality; keep explicit `not_collected` thread fingerprints only as a temporary marker until the finalizer fetches real review-thread fingerprints for every PR. | In Progress |
| 2026-07-12 | Public report QA | The 17:00 post-archive QA found that the HTML PR auto-link regex was broken by Python f-string braces, public JSON still exposed `comment_only`/sync internals, and generic not-reached wording made refreshed PRs look unexplained. | Keep the renderer regex and follow-up title fixes, extend public sanitization and forbidden-text checks for run/process terms, and add fixture coverage for public timeline/evolution/not-reached wording plus runtime PR-link rendering. | In Progress |
| 2026-07-13 | Report QA | The 01:00 post-archive QA found that the hand-written overview undercounted skipped groups and public JSON still exposed machine-facing `reason`/`kind` codes even though the HTML looked clean. | Generate overview counts from `skipped_groups`, keep a quality guard for overview/count consistency, and sanitize public `reason`/`kind` values into maintainer-facing labels before rendering. | In Progress |
| 2026-07-13 | Public report wording | The 09:03 post-archive QA found that the public report still rendered the internal status-refresh/not-reached bucket as `状态已刷新`, which looked like workflow machinery rather than a maintainer-facing reason. | Keep public reports phrased as maintainer outcomes, map unchanged/not-reached buckets to `等待新变化后复核`, and fail public artifact checks if `状态已刷新` reappears. | In Progress |
| 2026-07-13 | Review delegation resilience | The 17:04 comment-only run spawned parallel reviewers for changed candidates, but every reviewer stream disconnected before returning a result, so the main workflow had to complete the reviews locally and remember that fallback manually. | Persist reviewer attempt/result/fallback state per PR, mark disconnected attempts as non-authoritative, and make the report finalizer include the fallback path without exposing low-level orchestration text in public artifacts. | Open |
| 2026-07-13 | Final open PR diff | The 17:04 run found #2203 only during the final open-PR diff pass after the initial 82-PR ledger had already been built. | Make the finalizer always diff the final open PR list against the initial ledger, collect minimal metadata for late-new PRs, and fail report generation if any final open PR is missing from processed or skipped coverage. | Open |
| 2026-07-14 | GitHub collection resilience | The 01:12 run repeatedly hit EOF/framing failures while fetching long-lived old PR metadata and while posting one inline comment, requiring manual resume, minimal-index fallback, and HTTP/1.1 retry. | Promote the ad hoc resume logic into a collector/poster helper with per-PR cache checkpoints, bounded retries, HTTP/1.1 fallback for comment posting, success URL validation, and a report-visible but public-safe note when minimal metadata was reused. | Open |
| 2026-07-14 | Incremental planning | The 09:12 run initially promoted all 88 open PRs because older `run_state` entries used legacy `base`/`draft` fields and lacked ordinary labels, so unchanged PRs looked like base/readiness changes. | Normalize legacy `base`/`draft`/`wip` fields and make readiness compare state/draft/WIP rather than ordinary labels; keep WIP labels effective through normalized `is_wip`, and cover the compatibility path in planner tests. | Done |
| 2026-07-15 | GitHub collection resilience | The 01:28 run hit a `gh pr list --json statusCheckRollup` GitHub 502 and had to fall back to per-PR `gh pr checks` snapshots before candidate grouping. | Promote the fallback into a repo-managed collector step that uses bounded concurrency, records the primary API failure, stores normalized per-PR check summaries, and feeds the same schema to the planner/finalizer. | Open |
| 2026-07-15 | Report finalization | The 01:28 report initially had one manual-review overview count out of sync with the final `skipped_groups` after commented PRs were removed from skipped coverage. | Generate overview counts directly from final processed/skipped groups instead of hand-written numbers, and fail report generation when any overview count disagrees with the group data. | Open |
| 2026-07-15 | Public report hygiene | Post-archive QA found that public overview prose could expose local checkout dirty state even though private keys were stripped correctly. | Remove the current public prose leak and extend `report-quality.mjs` public forbidden-text checks for checkout/worktree dirty wording. | Done |
| 2026-07-15 | GitHub checks schema | The 09:28 run first requested `gh pr checks --json conclusion`, but the current CLI no longer exposes that field, requiring a second collection pass with supported fields. | Add a repo-managed checks collector that pins the supported `gh pr checks --json` field set, validates schema once at startup, and emits normalized check summaries for the planner/finalizer. | Open |
| 2026-07-15 | Review delegation queue | Parallel candidate review hit the active-agent limit, so #2183 had to be reviewed in the main workflow while other reviewers were still open. | Add a bounded review queue that closes completed reviewer sessions promptly and backfills the next candidate automatically, while recording fallback review evidence in the run artifacts. | Open |
| 2026-07-15 | Inline comment anchoring | `gh pr diff --patch` exposed repeated historical hunks for a multi-commit PR, so changed-line validation had to switch to the GitHub `pulls/files` patch before posting inline comments. | Build a `post-inline-comments` helper that derives anchors from `pulls/files`, verifies current head SHA immediately before posting, rejects stale/missing anchors, and stores created discussion URLs for report generation. | Open |
| 2026-07-15 | Comment finalization | The 17:28 comment-only run posted 13 inline comments successfully, but the head check, duplicate-comment check, anchor preflight, API posting, URL capture, and report stitching still lived in an ad hoc script. | Promote the script into a repo-managed `post-inline-comments` command that consumes structured findings, deduplicates against existing comments, validates anchors against `pulls/files`, posts through `gh api`, and emits report-ready comment records. | Open |
| 2026-07-15 | Existing feedback handling | #2137 still had the same `call_tool` permission/callback blocker in current code, but an existing maintainer comment already covered it, so posting another inline comment would duplicate review noise. | Add a first-class `existing_actionable_feedback` outcome that records the existing comment URL, verifies whether the current diff still contains the risk, and renders it separately from newly posted comments. | Open |

## Parking Lot

| Date | Area | Observation | Proposed Change | Status |
| --- | --- | --- | --- | --- |
| 2026-05-26 | Report quality | The archive index currently summarizes run-level data and links to full HTML. | Add a generated per-PR search index if the number of reports grows enough that cross-run PR lookup becomes slow. | Open |
| 2026-05-26 | CI visibility | GitHub Pages deploy status is separate from the local run report. | Add latest deploy/check status to README or the dashboard if it becomes useful. | Open |
